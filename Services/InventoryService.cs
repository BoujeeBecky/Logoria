// Replaced by MnemeInventoryService.
//
// The old implementation matched every Item whose name ended in "Logogram" and
// counted it with InventoryManager. That found the nine unidentified logogram
// containers rather than the 28 deciphered mnemes that recipes actually consume,
// and deciphered mnemes do not live in normal bags at all, which is why every row
// in the dex reported "(0 in bag)".
//
// This file is now intentionally empty and is safe to delete.
